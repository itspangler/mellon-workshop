[build]
  publish = "public"
  command = "hugo"

[build.environment]
  HUGO_VERSION = "0.145.0"
  HUGO_ENV = "production"

[context.deploy-preview]
  command = "hugo --buildFuture"

[context.branch-deploy]
  command = "hugo --buildFuture"